import Banner from '@components/Banner';
import Contact from '@components/Contact';

export default function Home() {
    return (
        <main className="relative overflow-hidden pt-[-128px]">
            <Banner />
            <Contact />
        </main>
    );
}
